# Roadmap

## v1 (shipped)

- Offline six-category scan + MoneyGap Score™ TUI
- JSON / Markdown / HTML reports
- Fix suggestions (no auto-patch)
- Plugin + AIProvider stubs
- Auth / update soft stubs

## Next

- Discover `moneygap-plugin-*` packages
- Optional Lighthouse / Chrome performance (opt-in)
- Safe `--apply` patches with dry-run diffs
- Real cloud auth + upload to MoneyGap dashboard
- Wired AI explainers behind API keys
- PDF reporter
- WordPress / Shopify adapters

## Not planned for early releases

- Coupling CLI runtime to the Next.js SaaS app process
